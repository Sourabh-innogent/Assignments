generator client {
  provider = "prisma-client-py"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:root@localhost:3306/filehandling"
}

model Client {
  id    Int    @id @default(autoincrement())
  name  String
  email String @unique
  about String?
}
